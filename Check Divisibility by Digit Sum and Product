def checkDivisibility(self, n: int) -> bool:
        str_n = str(n)

        sum_n = 0
        mul_n = 1

        for i in str_n:
            sum_n += int(i)
            mul_n *= int(i)

        return True if n % (sum_n + mul_n) == 0 else False
