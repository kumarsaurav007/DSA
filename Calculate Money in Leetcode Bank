def totalMoney(self, n: int) -> int:
        total = 0
        current = 1
        for day in range(1,n+1):
            total+=current
            if day % 7 == 0:
                current = day//7+1
            else:
                current+=1
        return total
