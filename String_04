#Number of Senior Citizens:-

def countSeniors(details):
        count = 0
        for i in details:
            age = (i[11:13])
            if int(age) > 60:
                count+=1
        return count
        
