# Minimum Operations to Exceed Threshold Value I :-

def minOperations(self, nums: List[int], k: int) -> int:
        res = 0
        for i in nums:
            if i < k:
                res += 1
        return res
