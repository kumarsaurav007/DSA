#Diagonal

def get_diagonal(tup):
    result = ()
    for i in range(len(tup)):
        result += (tup[i][i],)
    return result
input_tuple = (
    (1, 2, 3),
    (4, 5, 6),
    (7, 8, 9)
)
print(get_diagonal(input_tuple))
