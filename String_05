#Reverse Prefix of Word:-

def reversePrefix(word, ch):
        if ch not in word:
            return word
        else:
            index = word.index(ch)
            result = word[:index+1][::-1] + word[index+1:]
        return result
