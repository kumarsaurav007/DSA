# Count Negative Numbers in a Sorted Matrix :-

def countNegatives(self, grid: List[List[int]]) -> int:
        output = 0
        for i in grid:
            for j in i:
                if j < 0:
                    output += 1
        return output
