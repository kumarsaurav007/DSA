# Find Missing Elements :-

def findMissingElements(self, nums: List[int]) -> List[int]:
        x = min(nums)
        y = max(nums)

        ans = []

        for i in range(x, y):
            if i not in nums:
                ans.append(i)

        return ans
